<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.techkarkhana.motivateme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activityall.WelcomeSplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activityall.welcomeActivity2" />
        <activity android:name=".activityall.SignInActivity" />
        <activity android:name=".activityall.BreathWithMeActivity" />
        <activity android:name=".activityall.MazeOneActivity" />
        <activity android:name=".activityall.MazeTwoActivity" />
        <activity android:name=".activityall.QuestionPageOneActivity">

        </activity>
        <activity android:name=".activityall.HomePageActivity"></activity>
    </application>

</manifest>